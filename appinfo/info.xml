<?xml version="1.0"?>
<info xmlns:xsi= "http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
  <id>journeys</id>
  <name>Journeys</name>
  <summary>cluster images by time and location</summary>
    <description><![CDATA[
# Automatic Photo Album Creation for Nextcloud

Automatically cluster your images into journeys (vacations/trips) and create albums for each journey.
**Requires the Memories and Photos apps!**

> ### 🆕 New: Journals — your travel diary
> Keep a hand-written travel diary right inside Nextcloud. Create a Journal, add day entries (auto-filled with photos from that day), write your story, and curate the pictures. **Publish to a revocable public link** — a mobile-friendly timeline with locations, a route map, a visited countries & cities overview and a cover photo — or **share with other users and groups for full co-editing.** Find it in the new **Journeys** sidebar app.

## ✨ Features
- **🗺️ Location & Time Clustering:** Group images by when and where they were taken
- **🗂️ Album Creation:** Albums are created automatically for each journey
- **⚙️ Customizable:** Control minimum cluster size, time gap, and distance thresholds
- **🎥 Video Rendering:** Turn journey albums into MP4 highlight reels. Renders run as background jobs (the settings page returns instantly) and land in `Documents/Journeys Movies/`. Per-location subtitles fade in/out as the journey moves between places. Requires `ffmpeg` on the server.
- **📔 Journals (travel diary):** Keep a hand-written travel diary in the new **Journeys** sidebar app: create a Journal, add a day entry (auto-filled with up to 20 photos spread across that day), write text, curate the photos and add a caption to each. Photos stay in capture-time order, so pictures added by a collaborator merge into the day's timeline where they were actually taken. Publish a Journal to a revocable public link — a mobile-friendly timeline with locations, a visited countries/cities overview and a cover photo. Share a Journal with other users or groups for full co-editing — and each member can optionally let the others pick from their own photo library for the journal's dates. Mark a Journal as completed when you're home; it then shows its travel stats — days, countries, distance travelled and photo count.
- **📚 Mobile-friendly journey browser:** Compact card list with year, month and location filters plus a `▶ Watch` shortcut to existing rendered videos — built for instances with many journeys.
- **🧵 Smart Merging:** Adjacent clusters in the same country (within one week) are automatically stitched together, so multi-city road trips and long vacations with rest days stay as one journey
- **🧹 Auto-cleanup of empty albums:** Journey albums whose photos you've all deleted are removed automatically on the next clustering run, so empty placeholders don't pile up. Manual albums are never touched.
- **✏️ User-editable journey names:** Set a custom name (e.g. *Christmas 2024*, *Family reunion*, *Sabbatical*) on top of the auto-derived location/date title. Custom names become the card heading, the Photos album title, and the video title overlay — and they survive `--from-scratch` reclustering by matching old names to new clusters via file-ID overlap.

## 🚀 OCC Command Usage

    php occ journeys:cluster-create-albums <user>

## 🏠 Using your home location (default)
Journeys segments the timeline into **near home** and **away from home** and applies sensible defaults for each to capture local days vs. trips.

## ⏰ Runs automatically
A background job runs **daily** to cluster newly added photos, and you’ll receive a **notification** whenever new albums are created.

## 🏷️ How album names are created
Album titles are generated from the **resolved place name** and the **date range** of the cluster, e.g. `Iceland — Aug 12–19, 2024`.

]]></description>
  <changelog><![CDATA[
  <h3>0.32.0 (2026-08-06)</h3>
    <ul>
      <li><b>Journals — mark a trip as completed:</b> Finish a journal when you get home (and reopen it if you want to keep writing). Completed journals get a ✓ badge in the list, in the editor and on the public page. It is only a marker — a completed journal can still be edited.</li>
      <li><b>Journals — travel stats:</b> Every journal now shows how long it lasted, how many countries it covered, how far you travelled and how many photos it holds, in the journal list, the editor and on the shared page. The distance follows your geotagged days in order; journals without location data simply show no distance.</li>
    </ul>
  <h3>0.31.0 (2026-08-06)</h3>
    <ul>
      <li><b>Journals — shared photo libraries:</b> Any member of a shared journal can switch on <i>"Let the others pick from my photos"</i>. The other members' day picker then also shows that member's pictures, labelled with who they belong to, and they can be added to the journal like your own.</li>
      <li><b>Scoped and revocable:</b> Sharing your library only exposes photos taken within the journal's date range, only to that journal's members, and only until you switch it off again. Pictures already added to the journal stay.</li>
      <li><b>Journals editor:</b> The collaborators box is now visible to every member (that is where the switch lives). Adding and removing collaborators remains owner-only.</li>
    </ul>
  <h3>0.30.0 (2026-08-06)</h3>
    <ul>
      <li><b>Journals:</b> Photos in a day entry are now kept in <b>capture-time order</b>, so pictures added by collaborators merge into the day's timeline where they were actually taken instead of being appended at the end. Existing entries are re-ordered by the update.</li>
      <li><b>Journals editor:</b> Removed the manual left/right photo arrows — ordering is chronological and automatic.</li>
      <li><b>Fixed:</b> Adding a day no longer attaches video files. Memories also indexes videos, so a day's auto-fill could pick up e.g. a Google Camera motion clip (<code>*.TS.mp4</code>, <code>*.LS.mp4</code>) that has no preview and showed up as a black tile. Videos already attached stay until you remove them.</li>
    </ul>
  <h3>0.29.0 (2026-08-05)</h3>
    <ul>
      <li><b>Journals public page:</b> The timeline now starts with the <b>newest day first</b>, matching the editor. The oldest/newest toggle still flips it.</li>
      <li><b>Journals list:</b> A journal that has no day entries yet no longer sinks to the bottom of the list — journals are ordered by their travel dates, falling back to when they were created, so a freshly created journal appears at the top.</li>
      <li><b>Journals editor:</b> Several entries on the same date now read newest-added first, consistent with the public page.</li>
      <li><b>Fixed:</b> On the public journal page the "Shared from a Journeys travel diary" line was pinned to the bottom of the window and covered the travel map. It now sits at the end of the page.</li>
    </ul>
  <h3>0.28.1 (2026-07-09)</h3>
    <ul>
      <li><b>Fixed:</b> Nextcloud 34 clustering crashed with <code>Call to undefined method OC\Server::getDatabaseConnection()</code>. Database access now uses a dependency-injected <code>IDBConnection</code> instead of the server-container getter that Nextcloud 34 removed.</li>
      <li><b>Internal:</b> Replaced the deprecated <code>\OC::$server->query()</code> with <code>get()</code> in the list-clusters command.</li>
      <li><b>Packaging:</b> Declared the Photos app as a required dependency.</li>
    </ul>
  <h3>0.28.0 (2026-06-28)</h3>
    <ul>
      <li><b>Journals editor:</b> Added per-photo captions — click any attached photo to write a short caption. Captions appear on the public page beneath the photo and in the full-size lightbox, and are preserved when you reorder photos or re-pick a day's selection.</li>
      <li><b>Journals public page:</b> Increased the base font size for more comfortable reading.</li>
      <li><b>Journals public page:</b> Fixed the per-entry location label, which could render as unreadable low-contrast text (e.g. yellow on a colored background) on some themes. It is now a high-contrast pill that reads clearly on any theme.</li>
    </ul>
  <h3>0.27.1 (2026-06-26)</h3>
    <ul>
      <li><b>Internal:</b> Migrated controller route annotations (<code>@NoAdminRequired</code>, <code>@NoCSRFRequired</code>, <code>@PublicPage</code>) to PHP 8 attributes for forward-compatibility with future Nextcloud majors that drop docblock-annotation support. No behavior change.</li>
    </ul>
  <h3>0.27.0 (2026-06-26)</h3>
    <ul>
      <li><b>Compatibility:</b> Declare support for Nextcloud 34 (<code>max-version</code> bumped from 33 to 34).</li>
      <li><b>Dependencies:</b> Raise minimum PHP to 8.2 to match Nextcloud 34's supported PHP range.</li>
    </ul>
  <h3>0.26.1 (2026-06-26)</h3>
    <ul>
      <li><b>Journals public page:</b> Full-size lightbox images now lazy-load — each is fetched only when its photo is opened, instead of all downloading on initial page load. A long shared trip (e.g. 21 days with ~15 photos a day) no longer pulls tens of megabytes of hidden full-size images up front.</li>
    </ul>
  <h3>0.26.0 (2026-06-25)</h3>
    <ul>
      <li><b>Journals public page:</b> Added a travel map — an OpenStreetMap basemap with the route line and numbered stops between geolocated days (shown when 2+ entries have distinct locations). The image is rendered and cached on the server and served as a plain image, so the public page needs no JavaScript. The tile source is configurable via the <code>mapTileUrl</code> app config value.</li>
      <li><b>Journals public page:</b> Removed the Nextcloud logo and the "Nextcloud – a safe home…" footer for a cleaner shared page.</li>
      <li><b>Diary entry location:</b> Each day's representative coordinate is now a medoid (a real photo location) rather than the average of all photos, so days whose photos span a wide area place their map marker on land instead of in open water.</li>
      <li><b>Journals editor:</b> The new-day date defaults to today, so adding today's entry is a single click — the button reads "Add today" and switches to "Add day" once you pick another date. Entries are always listed newest day first.</li>
    </ul>
  <h3>0.25.1 (2026-06-25)</h3>
    <ul>
      <li><b>Journals public page:</b> Added an oldest/newest sort toggle for the day-by-day timeline.</li>
      <li><b>Mobile fixes:</b> Editor action buttons no longer overflow off-screen on narrow/mobile views (the header wraps and the title gets its own row); public page text is now readable in dark mode, with a tighter small-screen layout.</li>
    </ul>
  <h3>0.25.0 (2026-06-24)</h3>
    <ul>
      <li><b>New — Journals (travel diary):</b> A new <b>Journeys</b> app in the sidebar for keeping a travel diary alongside the auto-clustered albums. Create a Journal, add a day entry (auto-seeded with up to 20 photos spread evenly across that day), write journal text, and curate/reorder photos. Multiple entries per day allowed.</li>
      <li><b>Public sharing:</b> Publish a Journal to a revocable public link — a mobile-friendly timeline with per-day location labels, a visited countries/cities overview, a cover photo, an oldest/newest sort toggle, a prev/next photo lightbox, and OpenGraph tags for link previews. Photos are served only through the share token.</li>
      <li><b>Collaboration:</b> Share a Journal with other users or groups. Everyone can co-edit entries, text and photos; each person adds photos from their own library and anyone can remove. Managing collaborators, publishing and deleting stay owner-only.</li>
      <li><b>Place names:</b> Diary locations now prefer an English/Latin-script name (e.g. <i>Greece</i> instead of <i>Ελλάς</i>) and ignore timezone boundaries. This also improves clustering album and video names.</li>
    </ul>
  <h3>0.24.3 (2026-05-07)</h3>
    <ul>
      <li><b>Video rendering:</b> Per-location subtitles for multi-city trips now use city names instead of sub-city districts. Paris is captioned as <i>Paris</i> (not <i>13e arrondissement</i>), Frankfurt as <i>Frankfurt am Main</i> (not <i>Süd</i>). Single-city journeys still fall back to suburb-level so movement within one city keeps varying the caption.</li>
    </ul>
  <h3>0.24.2 (2026-05-07)</h3>
    <ul>
      <li><b>Settings:</b> Personal-settings sidebar entry now shows a globe icon (<code>actions/public.svg</code>) instead of the generic settings cog, so Journeys is easier to spot in the personal-settings menu.</li>
    </ul>
  <h3>0.24.1 (2026-05-07)</h3>
    <ul>
      <li><b>Settings:</b> Clustering settings (basics, near/away thresholds, video & home, save / start-clustering buttons) are now grouped under a collapsible <i>Clustering settings</i> header — collapsed by default so the journeys grid is visible without scrolling. Open/closed state persists in browser <code>localStorage</code> under <code>journeys.settingsExpanded</code>. The summary line shows the last clustering run timestamp at a glance.</li>
    </ul>
  <h3>0.24.0 (2026-05-07)</h3>
    <ul>
      <li><b>Settings:</b> User-editable journey names. Each journey card has a pencil button that opens a modal to set a custom name (e.g. <i>Christmas 2024</i>, <i>Family reunion</i>, <i>Sabbatical</i>) on top of the auto-derived location/date title. The custom name becomes the card heading, the Photos album title, and the video title overlay; the auto-derived name is kept as a secondary line for reference.</li>
      <li><b>Clustering:</b> Custom journey names survive <code>--from-scratch</code> reclustering. The clusterer snapshots each named album's file IDs before purge and re-attaches the name to whichever new album has the highest file-ID overlap (Jaccard &ge; 0.5). Names below threshold are dropped, so the system stays robust across algorithm changes, place-resolver improvements, and retroactive photo additions that shift cluster boundaries.</li>
      <li><b>DB migration:</b> New nullable <code>custom_name</code> column on <code>oc_journeys_cluster_albums</code>.</li>
    </ul>
  <h3>0.23.6 (2026-05-07)</h3>
    <ul>
      <li><b>Clustering:</b> Tracked albums whose photos have all been deleted are now removed automatically on the next clustering run (daily cron, OCC, or settings UI). Manual albums and tracked albums that still hold photos are left untouched. Closes <a href="https://github.com/thrillfall/journeys/issues/24">#24</a>.</li>
    </ul>
  <h3>0.23.5 (2026-05-06)</h3>
    <ul>
      <li><b>Video rendering:</b> Per-location subtitles now also overlay portrait videos (previously landscape-only). Same group/cap/single-location-suppression behavior; reuses the <code>showLocationSubtitles</code> toggle.</li>
    </ul>
  <h3>0.23.4 (2026-05-06)</h3>
    <ul>
      <li><b>Video rendering:</b> Landscape videos now show per-location subtitles that fade in/out per location group (capped at 5s). Suppressed automatically when the journey only resolves to one place. Toggle <i>Show per-location subtitles on videos</i> in personal settings (default on).</li>
    </ul>
  <h3>0.23.3 (2026-05-05)</h3>
    <ul>
      <li><b>Video rendering:</b> Landscape chunked merge no longer collapses output to a fraction of expected length. The merge step now probes each chunk's actual on-disk duration before computing <code>xfade</code> offsets; previously it trusted the renderer's formula-based duration which overstated chunks containing motion-photo segments shorter than their nominal slot. The inflated offsets pushed <code>xfade</code> past the end of the running merged stream and ffmpeg silently dropped subsequent chunks. Verified on a 96-image / 8-chunk cluster: output went from 57s to 225s (expected). Different layer than the 0.23.2 orientation-filter fix.</li>
    </ul>
  <h3>0.23.2 (2026-04-28)</h3>
    <ul>
      <li><b>Video rendering:</b> Landscape videos are no longer truncated. Image selection for the landscape renderer now pre-filters to landscape-orientation candidates instead of inheriting the portrait-biased mix from the user's <code>videoOrientation</code> setting. Previously a 120-image selection from a multi-week journey produced a ~1-minute landscape video because portrait frames were dropped after sampling, leaving only ~30–40 usable inputs.</li>
    </ul>
  <h3>0.23.1 (2026-04-27)</h3>
    <ul>
      <li><b>Video rendering:</b> Always chunk renders; chunk size adapts to the largest source image (16 / 12 / 8 / 4 segments per chunk for ≤8 / ≤16 / ≤32 / &gt;32 MP). Previously a 13 MP gate kept typical phone-camera albums (12 MP) on a single-pass path with 60+ <code>-loop 1 -i image.jpg</code> decoders open at once, which OOM-killed ffmpeg mid-album. Each chunk now runs as its own ffmpeg process so peak RSS is released between chunks. Verified end-to-end on an 88-image cluster.</li>
    </ul>
  <h3>0.23.0 (2026-04-26)</h3>
    <ul>
      <li><b>Settings:</b> Redesigned the journeys list as a compact, mobile-friendly card grid. Each card shows dates, photo count and place inline; the previous HTML tables are gone.</li>
      <li><b>Settings:</b> New filter toolbar above the journeys list — year, month, and free-text location/name search with a result counter, for managing instances with many journeys.</li>
      <li><b>Settings:</b> New "Rendered videos" section lists every <code>.mp4</code> in <code>Documents/Journeys Movies/</code> with mtime and size, plus an "Open in Files" link.</li>
      <li><b>Settings:</b> Per-journey <code>▶ Watch</code> badge appears when a rendered video exists for that cluster and links straight to it in the Files app.</li>
      <li><b>Video rendering:</b> Triggering a render from the settings page now enqueues a <code>RenderClusterVideoJob</code> instead of running ffmpeg synchronously inside the HTTP request, so the page returns instantly and you no longer have to keep the tab open through a multi-minute render.</li>
    </ul>
  <h3>0.22.4 (2026-04-26)</h3>
    <ul>
      <li><b>Video rendering:</b> Cap ffmpeg's decoder, filter, and encoder thread pools to 2 each (<code>-threads 2 -filter_threads 2 -filter_complex_threads 2</code>) to bound peak memory in <code>zoompan</code>/<code>xfade</code> filter graphs. Without this, ffmpeg defaults to <code>nproc</code> and concurrent full-resolution frame buffers had OOM-killed or frozen multi-core servers rendering large albums.</li>
    </ul>
  <h3>0.22.3 (2026-04-25)</h3>
    <ul>
      <li><b>Video rendering:</b> Per-cluster image cap now scales with trip length so multi-week journeys produce longer recap videos. The previous fixed default of 80 images stays in effect for trips up to a week, then climbs by 4 per extra day up to an absolute cap of 120 (≈3:30 at the default 2.5 s per image). <code>--max-images</code> still acts as an explicit override.</li>
    </ul>
  <h3>0.22.2 (2026-04-23)</h3>
    <ul>
      <li><b>Clustering:</b> Merge pass now absorbs tiny GPS-noise clusters (below <code>minClusterSize</code>) that sit between two same-country clusters, so a bogus single-photo coordinate in the middle of a trip no longer prevents the trip from being stitched. Observed on a New Zealand journey that split around two 1–2 image clusters with device-default Hong Kong coordinates.</li>
      <li><b>Clustering debug:</b> <code>--debug-splits</code> now prints <code>MERGE (through noise)</code> with the absorbed cluster's size, fileid, datetime, and coordinates.</li>
    </ul>
  <h3>0.22.1 (2026-04-23)</h3>
    <ul>
      <li><b>Clustering debug:</b> <code>--debug-splits</code> now prints <code>NO MERGE</code> events when the merge pass rejects a pair on country grounds (null on either side, or name mismatch), including resolved country strings, image coordinates, and fileids. Helps diagnose cases where adjacent clusters in the same country are not being stitched.</li>
    </ul>
  <h3>0.22.0 (2026-04-23)</h3>
    <ul>
      <li><b>Compatibility:</b> Declare support for Nextcloud 33 (<code>max-version</code> bumped from 32 to 33).</li>
    </ul>
  <h3>0.21.0 (2026-04-22)</h3>
    <ul>
      <li><b>Clustering:</b> New post-clustering merge pass stitches adjacent clusters in the same country within 7 days, fixing over-splitting of multi-city road trips and long vacations with rest days. Away-from-home only; near-home clusters never merge.</li>
      <li><b>Settings:</b> New "Merge adjacent journeys in the same country (within one week)" toggle (default on).</li>
      <li><b>CLI:</b> New <code>--no-merge</code> flag; <code>--debug-splits</code> now prints <code>MERGE</code> events.</li>
    </ul>
  <h3>0.20.3 (2026-04-22)</h3>
    <ul>
      <li><b>Video rendering:</b> Extracted shared primitives from portrait and landscape renderers into a new <code>VideoRenderPrimitives</code> trait (~350 LOC duplication removed). No behavior change; both orientations verified end-to-end.</li>
    </ul>
  <h3>0.20.2 (2026-02-13)</h3>
    <ul>
      <li><b>Cron:</b> Reduced <code>recentCutoffDays</code> from 5 to 2 in the daily clustering job to align with the OCC command default.</li>
    </ul>
  <h3>0.20.1 (2026-02-13)</h3>
    <ul>
      <li><b>Fix:</b> Moved <code>VideoRenderJobScheduler</code> service definition inside the XML structure in <code>services.xml</code> so auto-generated video rendering jobs are correctly scheduled by the daily clustering cron.</li>
    </ul>
  <h3>0.20.0 (2026-02-10)</h3>
    <ul>
      <li><b>Clustering:</b> Add optional date range scoping (<code>from/to</code>) to limit which photos are clustered.</li>
      <li><b>UI:</b> Personal Settings now let you set an optional "Only cluster from/to" range; daily cron honors it.</li>
      <li><b>CLI:</b> Add <code>--from</code>, <code>--to</code>, and <code>--last-years</code> options; if no CLI range is provided, the command falls back to the user's configured UI range.</li>
    </ul>
  <h3>0.19.6 (2026-01-05)</h3>
    <ul>
      <li><b>Cron:</b> Daily clustering job now logs “no images found for user” as info instead of warning.</li>
    </ul>
  <h3>0.19.5 (2025-12-18)</h3>
    <ul>
      <li><b>Clustering fix:</b> When <code>--include-shared-images</code> is enabled, only include images from <i>incoming</i> shares (exclude outbound shares shared by the user).</li>
    </ul>
  <h3>0.19.4 (2025-12-18)</h3>
    <ul>
      <li><b>Clustering:</b> Exclude generated videos in <code>Documents/Journeys Movies</code> from clustering input.</li>
      <li><b>Clustering:</b> More robust timestamp parsing & deterministic sorting to avoid spurious splits when including shared images.</li>
      <li><b>Clustering debug:</b> <code>--debug-splits</code> output now includes raw boundary indices plus source (<code>home/shared</code>) and location context (<code>loc=</code>, <code>prevGeo</code>).</li>
    </ul>
  <h3>0.19.3 (2025-12-17)</h3>
    <ul>
      <li><b>Clustering debug:</b> New CLI flag <code>--debug-splits</code> prints why clustering starts a new cluster (time/distance exceeded amounts and home-aware boundaries).</li>
    </ul>
  <h3>0.19.2 (2025-12-17)</h3>
    <ul>
      <li><b>Clustering fix:</b> Shared image inclusion is now correctly scoped to the shared mount root subtree (prevents pulling unrelated files from other users' storages).</li>
    </ul>
  <h3>0.19.1 (2025-12-16)</h3>
    <ul>
      <li><b>Clustering debug:</b> When clustering with shared images enabled, the OCC output now prints which shared images were included per created cluster (fileid, path, datetaken, datetaken_ts).</li>
    </ul>
  <h3>0.19.0 (2025-12-16)</h3>
    <ul>
      <li><b>Video rendering:</b> Verified compatibility with FFmpeg 7.x (CFR handling and fades); recommend FFmpeg 7.1+.</li>
    </ul>
  <h3>0.18.3 (2025-12-09)</h3>
    <ul>
      <li><b>Video rendering:</b> Landscape renderer now chunks like portrait when any source image exceeds 13 MP; otherwise it runs a single pass. Chunk/merge progress logging aligned with portrait.</li>
    </ul>
  <h3>0.18.2 (2025-12-09)</h3>
    <ul>
      <li><b>Video rendering:</b> Portrait renderer chunks automatically when any source image exceeds 13 MP; otherwise it runs a single pass.</li>
    </ul>
  <h3>0.18.1 (2025-12-05)</h3>
    <ul>
      <li><b>Settings UI:</b> Redesigned the personal settings page with Nc components, card layout, and inline auto-saving toggles.</li>
    </ul>
  <h3>0.18.0 (2025-12-05)</h3>
    <ul>
      <li><b>Videos:</b> Manual Photos albums can now be rendered from the settings UI or CLI with their actual album titles.</li>
      <li><b>CLI:</b> <code>journeys:list-clusters</code> prints both journey clusters and manual albums in a formatted table.</li>
    </ul>
  <h3>0.17.2 (2025-12-05)</h3>
    <ul>
      <li><b>Clustering:</b> OCC command now streams cluster creation details as albums are created.</li>
    </ul>
  <h3>0.17.1 (2025-12-04)</h3>
    <ul>
      <li><b>Albums:</b> Detect and skip screenshot-style images (no EXIF, PNG/WebP, screen keywords) during clustering.</li>
    </ul>
  <h3>0.17.0 (2025-12-04)</h3>
    <ul>
      <li><b>Video rendering:</b>Prefer fotos with faces for video generation</li>
      <li><b>Settings:</b> "Prefer photos with people when building videos" now remembers its saved value after refresh.</li>
    </ul>
  <h3>0.16.0 (2025-11-28)</h3>
    <ul>
      <li><b>Shared images:</b> Personal settings toggle now includes shared-by-others mounts in clustering and video selection.</li>
      <li><b>Diagnostics:</b> OCC clustering command prints per-source image counts (home, group, shared) to help verify coverage.</li>
      <li><b>UI:</b> Personal settings warn when shared inclusion yields no matches.</li>
    </ul>
  <h3>0.15.1 (2025-11-24)</h3>
    <ul>
      <li><b>Fallback:</b> Place resolution now automatically falls back to Memories tables when GIS functions are unavailable or fail.</li>
      <li><b>Upgrade note:</b> Drop existing Journeys albums and re-run clustering to rebuild album titles with the new fallback.</li>
    </ul>
  <h3>0.15.0 (2025-11-24)</h3>
    <ul>
      <li><b>Selection:</b> Video image selection now prefers photos with faces when Recognize data is available.</li>
      <li><b>Settings:</b> Added personal setting “Prefer photos with people when building videos” (enabled by default).</li>
      <li><b>CLI:</b> Added <code>--no-face-boost</code> flag to render commands to disable face boosting per run.</li>
    </ul>
  <h3>0.14.0 (2025-11-20)</h3>
    <ul>
      <li><b>Feature:</b> Videos now display cluster name as title overlay on the first image (4 seconds with fade in/out). Font size automatically scales to fit 80% of video width.</li>
      <li><b>UI:</b> Added "Show cluster name title on videos" checkbox in Personal Settings to toggle title display (enabled by default).</li>
      <li><b>CLI:</b> Added <code>--no-title</code> flag to both render commands to disable title overlay.</li>
    </ul>
  <h3>0.13.2 (2025-11-20)</h3>
    <ul>
      <li><b>Fix:</b> Video rendering no longer hangs when processing GCam motion clips. Fixed frame padding calculation to use exact frame counts.</li>
      <li><b>Change:</b> Rendered videos now always include timestamp suffix to avoid file conflicts.</li>
    </ul>
  <h3>0.13.1 (2025-11-19)</h3>
    <ul>
      <li><b>Feature:</b> Added <code>--ffmpeg-verbose</code> CLI flag to both render commands for debugging.</li>
    </ul>
  <h3>0.13.0 (2025-11-19)</h3>
    <ul>
      <li><b>Fix:</b> Ken Burns effect now works correctly on still images following GCam motion videos in both portrait and landscape renderers (PTS normalization).</li>
    </ul>
  <h3>0.12.1 (2025-11-19)</h3>
    <ul>
      <li><b>Fix:</b> <code>journeys:remove-all-albums</code> now only removes Journeys-created albums, preserving manually-created albums.</li>
    </ul>
  <h3>0.12.0 (2025-11-18)</h3>
    <ul>
      <li><b>Video:</b> Landscape video renderer now supports GCam Motion Photos (Live Photos).</li>
      <li><b>CLI:</b> Added <code>--no-motion</code> flag to <code>journeys:render-cluster-video-landscape</code> command.</li>
    </ul>
  <h3>0.11.1 (2025-11-13)</h3>
    <ul>
      <li><b>Meta:</b> Patch version bump.</li>
    </ul>
  <h3>0.11.0 (2025-11-13)</h3>
    <ul>
      <li><b>Naming:</b> Near-home clusters now append frequent sublocality names to make album titles more specific.</li>
    </ul>
  <h3>0.9.2 (2025-11-06)</h3>
    <ul>
      <li><b>Notifications:</b> Don't filter out notifications from other apps.</li>
    </ul>
  <h3>0.9.1 (2025-11-05)</h3>
    <ul>
      <li><b>UX:</b> Time thresholds in Personal Settings are now configured in hours (allowing decimals, e.g. 0.5 = 30m).</li>
      <li><b>Docs:</b> Brief documentation update to reflect the change.</li>
    </ul>
  <h3>0.9.0 (2025-11-05)</h3>
    <ul>
      <li><b>UI:</b> Separate, clearly grouped near-home and away-from-home thresholds in Personal Settings.</li>
      <li><b>CLI:</b> When arguments are omitted, OCC now falls back to the user's saved settings (including near/away thresholds) and prints the effective settings at the start of a run.</li>
      <li><b>Cron:</b> Daily clustering job honors per-user settings from the UI for both global and near/away thresholds.</li>
      <li><b>Debug logging:</b> Added detailed logs for cluster splits (time vs distance) and per-segment thresholds (near/away) at debug level.</li>
    </ul>
  <h3>0.8.2 (2025-11-04)</h3>
    <ul>
      <li><b>Group Folders (optional):</b> Added UI toggle and <code>--include-group-folders</code> CLI flag to include photos from Group Folders and other mounts in clustering (default: off).</li>
      <li><b>Album assignment:</b> Albums now add photos by <code>fileid</code> (mount-agnostic) instead of path, fixing empty albums when clustering Group Folder images.</li>
    </ul>
  <h3>0.8.1 (2025-11-01)</h3>
    <ul>
      <li>Latest-first cluster order in settings; minor UI polish; internal job scheduling refactor.</li>
    </ul>
  <h3>0.7.9 (2025-10-14)</h3>
    <ul>
      <li><b>Audio:</b> Updated soundtrack sources to 128kbps and added gentle fade‑out at the end of videos.</li>
    </ul>
  <h3>0.7.8 (2025-10-12)</h3>
    <ul>
      <li><b>Selection:</b> Coverage-first, two-pass image selection spreads picks across the whole journey.</li>
    </ul>
  <h3>0.7.5 (2025-10-10)</h3>
    <ul>
      <li><b>Video stacks:</b> Portrait videos now occasionally include <i>3-wide</i> landscape stacks with horizontal slides.</li>
    </ul>
  <h3>0.7.4 (2025-10-10)</h3>
    <ul>
      <li><b>Video stacks:</b> Portrait videos now occasionally include <i>3-wide</i> landscape stacks with horizontal slides.</li>
      <li><b>Pacing:</b> Each stacked row pauses at center (~2s) with quicker slide-in/out for a more dynamic feel.</li>
      <li><b>Selection:</b> Video selection no longer filters out landscape images; renderer mixes them automatically.</li>
    </ul>
  <h3>0.7.3 (2025-10-07)</h3>
    <ul>
      <li><b>CLI cleanup:</b> Removed the deprecated <code>--home-aware</code> flag; home-aware clustering remains enabled by default and can still be disabled with <code>--no-home-aware</code>.</li>
      <li><b>Granularity:</b> Added <code>--min-cluster-size</code> option to control the minimum images required for journey albums (replaces the positional argument) and raised the default to 5 photos.</li>
      <li><b>Recency threshold:</b> Default skip window for recent clusters is now 48 hours (was 5 days) to surface new journeys sooner.</li>
    </ul>
    <h3>0.7.2 (2025-09-29)</h3>
    <ul>
      <li><b>Audio:</b> Journey videos now include (one of three randomly selected) soundtrack by mobygratis .</li>
    </ul>
    <h3>0.7.1 (2025-09-26)</h3>
    <ul>
      <li><b>Improvement:</b> Added Ken Burns motion with crossfades</li>
    </ul>
    <h3>0.7.0 (2025-09-26)</h3>
    <ul>
      <li><b>New:</b> Render Journey albums into MP4 videos directly from the personal settings page or via <code>occ journeys:render-cluster-video &lt;user&gt; &lt;albumId&gt;</code>.</li>
      <li><b>Requirement:</b> The Nextcloud server must have <code>ffmpeg</code> available in <code>PATH</code> for rendering to succeed.</li>
    </ul>
    <h3>0.6.0 (2025-09-19)</h3>
    <ul>
      <li><b>Packaging:</b> Release workflow now builds the frontend before packaging to ensure fresh assets are shipped.</li>
    </ul>
    <h3>0.5.8 (2025-09-19)</h3>
    <ul>
      <li><b>Settings (#7):</b> Personal settings page is now accessible to all logged-in users (per-user configuration; no admin required).</li>
      <li><b>Security:</b> Re-enabled CSRF protection on settings endpoints (removed <code>@NoCSRFRequired</code> annotations).</li>
    </ul>
    <h3>0.5.7 (2025-09-15)</h3>
    <ul>
      <li><b>Meta:</b> Version bump.</li>
    </ul>
    <h3>0.5.6 (2025-09-15)</h3>
    <ul>
      <li><b>Cleanup:</b> Removed legacy <code>appinfo/app.php</code> since the app uses <code>IBootstrap</code> via <code>lib/AppInfo/Application.php</code>. This eliminates the recurring log warning about <code>app.php</code> not being loaded.</li>
    </ul>
    <h3>0.5.5 (2025-09-10)</h3>
    <ul>
      <li><b>Default behavior change:</b> Clustering now uses your home location by default to segment near-home vs away-from-home and apply appropriate thresholds.</li>
      <li><b>New flag:</b> <code>--no-home-aware</code> to disable home-based segmentation and use a single global set of thresholds.</li>
      <li><b>Deprecated:</b> <code>--home-aware</code> (no longer required; default is on).</li>
      <li><b>Docs:</b> Updated description to explain how home location is used and how to opt out.</li>
    </ul>
    <h3>0.5.4 (2025-09-09)</h3>
    <ul>
      <li><b>Notifications:</b> Send a single aggregated notification per run (instead of one per album) with a quick link to open Photos.</li>
    </ul>
    <h3>0.5.3 (2025-09-08)</h3>
    <ul>
      <li><b>DB migration:</b> Added <code>start_dt</code> and <code>end_dt</code> columns plus index to <code>journeys_cluster_albums</code> for reliable incremental clustering.</li>
      <li><b>Fail-fast tracking:</b> Album tracking now throws on DB errors instead of silently ignoring them.</li>
    </ul>
    <h3>0.5.2 (2025-09-08)</h3>
    <ul>
      <li><b>Fix:</b> Prevent division by zero when interpolating locations for adjacent images with identical datetaken.</li>
    </ul>
    <h3>0.5.1 (2025-09-07)</h3>
    <ul>
      <li><b>Album creation safety:</b> No longer reuse existing albums by name. On name collision (e.g., manually created album), the cluster album is skipped and a warning is logged.</li>
      <li><b>Incremental fallback:</b> When no <code>end_dt</code> is recorded yet, derive the latest cluster end from tracked albums to set the incremental boundary (no title matching required).</li>
    </ul>
    <h3>0.5.0 (2025-09-07)</h3>
    <ul>
      <li><b>Incremental clustering (default):</b> Only process images taken after the latest tracked cluster end.</li>
      <li><b>New OCC flags:</b> <code>--from-scratch</code> to fully rebuild clusters; <code>--recent-cutoff-days</code> to control skipping of very recent trips (default 5; 0 disables).</li>
      <li><b>DB migration:</b> Track <code>start_dt</code>/<code>end_dt</code> for each clusterer-created album to support incremental runs.</li>
    </ul>
    <h3>0.4.3 (2025-09-05)</h3>
    <ul>
      <li><b>Skip no-location-only clusters:</b> Clusters composed entirely of images without coordinates are no longer turned into albums. Reduces noise from placeholder “Journey # (date range)” albums.</li>
    </ul>
    <h3>0.4.2 (2025-09-04)</h3>
    <ul>
      <li><b>Clustering robustness:</b> Prevent time-only tails (images without coordinates) from bridging over large spatial jumps by anchoring distance checks to the last-known geolocated photo within a cluster.</li>
    </ul>
    <h3>0.4.1 (2025-09-03)</h3>
    <ul>
      <li><b>Deterministic purge of cluster albums:</b> Introduced DB-backed tracking table for clusterer-created albums (per user), used during purge instead of folder tag/title heuristics. Prevents accumulation of albums across runs.</li>
      <li><b>Docs:</b> Clarified deterministic purge behavior on re-runs; added upgrade notes.</li>
      <li><b>Cleanup:</b> Removed runtime table creation; rely on app migration for schema.</li>
      <li><b>DB prefix fix:</b> Use <code>*PREFIX*</code> placeholder in SQL instead of accessing connection prefix.</li>
    </ul>
    <h3>0.4.0 (2025-09-03)</h3>
    <ul>
      <li><b>Home-aware clustering:</b> Segment timeline into near-home and away-from-home blocks; cluster each block independently.</li>
      <li><b>Near-home alignment:</b> when left at built-in defaults, near-home time gap aligns to global, and distance aligns to global but is capped at <b>25km</b> for finer local clustering.</li>
      <li><b>Away thresholds</b> default to 36h / 50km and can be tuned via CLI to better handle multi-day gaps during long trips.</li>
    </ul>
    <h3>0.3.2 (2025-07-27)</h3>
    <ul>
      <li><b>Default maxDistanceKm for clustering is now 50.0 (was 100.0)</b></li>
    </ul>

    <h3>0.3.1 (2025-07-20)</h3>
    <ul>
      <li><b>SystemTag-only album identification:</b> New albums are created without postfix, identified and managed solely via SystemTags.</li>
      <li><b>Legacy albums with postfix are still purged via fallback logic.</li>
      <li><b>Removal of postfix from new album names.</li>
    </ul>
    <h3>0.3.0-alpha (2025-07-20)</h3>
    <ul>
      <li><b>Spatial constraint for interpolation:</b> Only interpolate image locations if the two neighboring images are within 1km of each other.</li>
      <li><b>Stricter time gap for single neighbor:</b> When only one reference image is available, the time gap must not be larger than 1 hour.</li>
      <li><b>Improved clustering accuracy:</b> Prevented assignment of interpolated locations over large distances.</li>
    </ul>
  ]]></changelog>
  <licence>agpl</licence>
  <author mail="thrillfall@disroot.org">thrillfall</author>
  <version>0.32.0</version>
  <namespace>Journeys</namespace>
  <category>integration</category>
  <category>multimedia</category>
  <screenshot>https://raw.githubusercontent.com/thrillfall/journeys/main/appinfo/journeys.png</screenshot>
  <bugs>https://github.com/thrillfall/journeys/issues</bugs>
  <dependencies>
    <nextcloud min-version="30" max-version="34"/>
  </dependencies>
  <bootstrap>OCA\Journeys\AppInfo\Application</bootstrap>
  <commands>
    <command>OCA\Journeys\Command\ClusterAndCreateAlbumsCommand</command>
    <command>OCA\Journeys\Command\RemoveAllAlbumsCommand</command>
    <command>OCA\Journeys\Command\ShowLatestClusterEndCommand</command>
    <command>OCA\Journeys\Command\RenderClusterVideoCommand</command>
    <command>OCA\Journeys\Command\RenderClusterVideoLandscapeCommand</command>
    <command>OCA\Journeys\Command\ListClustersCommand</command>
  </commands>
  <background-jobs>
    <job>OCA\Journeys\BackgroundJob\DailyClusteringJob</job>
  </background-jobs>
  <settings>
    <personal>OCA\Journeys\Settings\PersonalSettings</personal>
    <personal-section>OCA\Journeys\Sections\PersonalSection</personal-section>
  </settings>
  <navigations>
    <navigation>
      <name>Journeys</name>
      <route>journeys.page.index</route>
      <icon>app.svg</icon>
      <order>80</order>
    </navigation>
  </navigations>
</info>
